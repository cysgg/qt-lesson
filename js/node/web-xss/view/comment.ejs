<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
</head>
<body>
  <textarea name="" id="txt" cols="30" rows="10"></textarea>
  <button id="comment">comment</button>
  <button id="getcomment">getcomment</button>
  <script>
    const comment = document.getElementById('comment')
    const getcomment = document.getElementById('getcomment')
    const text = document.getElementById('txt')
    const xhrHander = function (type, url) {
      const xhr = new XMLHttpRequest()
      xhr.onreadystatechange = function () {
        // 0 1 2 3 4
        if (xhr.readyState === 4) {
          if (xhr.status == 200) {
            const data = xhr.response
            const parseData = JSON.parse(data)
            
          }
        }
      }
      xhr.open(type, url, true)
      xhr.send()
    }
    comment.addEventListener('click', xhrHander('get', '/comment?comment=' + txt.value))
    getcomment.addEventListener('click',xhrHander('get', '/getcomment'))
  </script>
</body>
</html>