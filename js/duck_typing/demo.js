const chior = Array();

function joinChior(animal){
    //check
    //add list
    //on num,sublmit\
    chior.push(animal)
}

// talk in js

const duck = {
    type : 'duck',
    duckSing : function(){
        console.log('gagaga');
        
    }
}

joinChior(duck);